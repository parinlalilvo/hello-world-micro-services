version: '3.7'
services:
  hello-world:
    build: .
    ports:
      - 8080:6969
    depends_on:
      - postgres_db
  postgres_db:
    image: postgres:11
    ports:
      - 5432:5432
    volumes: 
      - $PWD/init.sql:/docker-entrypoint-initdb.d/init.sql
